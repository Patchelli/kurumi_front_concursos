import type { ReactNode } from 'react';
type Props={open:boolean;title:string;description?:string;icon?:ReactNode;children?:ReactNode;actions:ReactNode;onClose():void;size?:'small'|'medium';scrimClassName?:string};
export function MaterialDialog({open,title,description,icon,children,actions,onClose,size='small',scrimClassName}:Props){if(!open)return null;return <div className={`md-dialog-scrim${scrimClassName ? ` ${scrimClassName}` : ''}`} role="presentation" onMouseDown={onClose}><section className={`md-dialog md-dialog-${size}`} role="dialog" aria-modal="true" aria-labelledby="dialog-title" onMouseDown={event=>event.stopPropagation()}>{icon&&<div className="md-dialog-icon">{icon}</div>}<header><h2 id="dialog-title">{title}</h2>{description&&<p>{description}</p>}</header>{children&&<div className="md-dialog-content">{children}</div>}<footer>{actions}</footer></section></div>}
