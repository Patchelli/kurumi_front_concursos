# Kurumí Frontend

Frontend React/TypeScript com arquitetura baseada no Kurumi Material e interface Material Design 3.

## Executar

```powershell
npm install
npm run dev
```

Por padrão, o frontend utiliza a API em `http://localhost:5080/api`. Use `VITE_API_URL` para alterar o endereço.
